# ═══════════════════════════════════════════════════
# .neuralmemory/surface.nm
# Neural Memory Knowledge Surface v1
# Auto-generated from brain.db | Editable by human
# ═══════════════════════════════════════════════════
---
brain: default
updated: 2026-07-09T04:57:05
neurons: 402
synapses: 3372
token_budget: 1200
depth_available: [surface, detail, deep]
---

# ── GRAPH ──────────────────────────────────────────
# Nodes = key concepts. Edges = causal/decisional links.
# Agent: scan this section to understand the knowledge landscape.
# Format: [id] content (type) {p:N} →edge→ [target_id_or_text]

[c15] Insight: a 6px submenu offset plus mouseleave close handler (concept) {p:6}
  →stored_by→ [c15] "Insight: a 6px submenu offset plus mouseleave close handler"
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →related_to→ "submenu offset"

[c16] Insight: static scan title/description outside the hidable scan view plus… (concept) {p:6}
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →related_to→ "scan title"
  →stored_by→ [c16] "Insight: static scan title/description outside the hidable…"
  →related_to→ "insight static"
  →related_to→ "用户于 2026-06-30 卸载了本机"小药经"桌面应用 (/Applications/小药经.app,…"

[c17] Error: making the sidebar collapse width 60px and centering the collapsed logo… (concept) {p:6}
  →related_to→ "sidebar collapse"
  →related_to→ [c28] "Decision: task board should not persist for…"
  →stored_by→ [c17] "Error: making the sidebar collapse width 60px and…"
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →co_occurs→ [c18] "Decision: task board should not persist for…"

[c18] Decision: task board should not persist for completed/failed/stopped history … (concept) {p:6}
  →related_to→ [c28] "Decision: task board should not persist for…"
  →co_occurs→ [c28] "Decision: task board should not persist for…"
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →co_occurs→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →stored_by→ [c18] "Decision: task board should not persist for…"

[c19] Insight: that paused tingwu websocket sessions could close and worker resume… (concept) {p:6}
  →related_to→ [c28] "Decision: task board should not persist for…"
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →co_occurs→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →co_occurs→ [c18] "Decision: task board should not persist for…"
  →stored_by→ [c19] "Insight: that paused tingwu websocket sessions could close…"

[c1] XYJ-Agent 桌面端顶部上下文：根因是旧 header 用 VIEW_META 静态写入 breadcrumb，修复为… (concept) {p:7}
  →stored_by→ [c1] "XYJ-Agent 桌面端顶部上下文：根因是旧 header 用 VIEW_META 静态写入…"
  →related_to→ "QR"
  →related_to→ [c28] "Decision: task board should not persist for…"
  →related_to→ [c5] "小药经资料模块：删除了 console.js 中重复的第二套 Finder 实现（~200行），修复了…"
  →related_to→ "Agent"

# ── SIGNALS ────────────────────────────────────────
# Active alerts, unresolved items, pending decisions.
# Agent: check this FIRST each session for urgent context.

! React `/settings/general` and `/settings/appearance` are implemented as basic pr
! XYJ-Agent React migration now has tested shell/theme/auth foundations: legacy th
! Chose React + Tiptap with route-level lazy chunks for XYJ-Agent because the loca
! XYJ-Agent React migration is staged behind `XYJ_REACT_FRONTEND`: legacy `/chat`
! 小药经/XYJ-Agent 调研：本机 Codex.app 是 Electron 42 + Vite webview + 原生 codex/codex_chro
! 小药经/XYJ-Agent 调研：主流 Agent 对话框已从单纯聊天输入演进为任务控制台，核心能力包括 context chips、计划/进度、工具调用事件、
~ XYJ-Agent sidebar unread bug: SSE done already called _markSidebarSessionDone, b
! Scratch: starting XYJ-Agent sidebar unread-state task; need inspect generation c
! Decision: .xiaoyaojing agent_workspace project-folder app internal directory | Decision:…
! Error: adding hidden/aria-hidden to #settings-sidebar and toggling that state in | Error: adding…

# ── DEPTH MAP ──────────────────────────────────────
# When surface isn't enough, this tells agent WHERE to dig.
# Format: [id] → depth_hint (context)

[c1] → NEEDS_DEEP (20 synapses in brain.db, recall "XYJ-Agent 桌面端顶部上下文：根因是旧 header 用…")
[c15] → NEEDS_DEEP (10 synapses in brain.db, recall "Insight: a 6px submenu offset plus…")
[c17] → NEEDS_DETAIL (12 total synapses, recall "Error: making the sidebar collapse…")
[c19] → NEEDS_DETAIL (11 total synapses, recall "Insight: that paused tingwu websocket…")

# ── META ───────────────────────────────────────────
# Stats for agent to gauge brain richness

coverage: 0.07
staleness: 0.14
last_consolidation: 2026-07-09T04:57:05
top_entities: [XYJ-Agent, XYJ-Agent, XYJ-Agent, XYJ-Agent, 小药经资料模块：删除了]
